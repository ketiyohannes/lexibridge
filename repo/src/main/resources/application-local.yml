spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:lexibridge}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USER:lexibridge}
    password: ${DB_PASSWORD:lexibridge}

lexibridge:
  security:
    antivirus:
      enabled: ${ANTIVIRUS_ENABLED:false}
